# 交接文档

给接手这个仓库的下一个会话（人或 AI）看。**先读这一页，再看代码。**

当前版本 **v0.5.0**。上一个稳定点是 `ad8fb60`（v0.4.1）。

---

## 一、这个项目是什么

把多个 AI 中转站聚合成**一个入口**：

- 客户端只配 `一个 Base URL + 一个 Key + 一个模型名`（比如 `auto`）
- 后面用哪个站、哪个真实模型，由后台决定
- 带失败自动切换、节点级冷却、每日限额、响应缓存、四种协议入口

| 项 | 值 |
|---|---|
| 仓库 | `caojian126/relayhub` |
| 技术栈 | Python 3.11 · FastAPI · httpx · SQLite(WAL) · 单文件 `index.html`（零构建） |
| 作者 | 草翦 |
| 部署 | Zeabur，**持久卷必须挂到 `/data`**，环境变量 `ADMIN_PASSWORD` / `SECRET_KEY` / `TZ` |

四个入口：`/v1/chat/completions`、`/v1/responses`、`/v1/messages`、`/v1beta/models/{m}:generateContent`。

---

## 二、v0.5.0 这一轮改了什么

从 `ad8fb60` 到 `c626d05` 共 11 个提交，按依赖顺序：

| 提交 | 内容 |
|---|---|
| `a9f805f` | **统一模型**（核心）＋ 节点级轮询/冷却 ＋ 流式边界处理 ＋ 导出导入分档 |
| `5a2d48d` | 密钥默认打码 + 主动显示；新增聊天测试区（走本机网关） |
| `14b488b` | 持久化警告横幅；导出分「安全/完整」，导入分「合并/覆盖」 |
| `32293c4` | README 补统一模型用法与测试说明，修 DEPLOY.md 乱码 |
| `3099984` | DEPLOY.md 新增统一模型章节、持久卷生效验证、更新流程 |
| `c16247d` | 拉模型改为**按站点勾选导入**，默认不勾选 |
| `8754971` | **自动同步默认关闭** + 路由批量编辑 |
| `c7589b5` | 自动生成的分组不再塞满统一模型页，也不再默认暴露 |
| `db55ab2` | **批量挑选模型加节点**（不用手打上游模型名） |
| `ffc062d` | 挑节点默认只列「已导入的模型」 |
| `c626d05` | 改面板账号名后右上角立刻更新 |

### 核心概念：统一模型

```
统一模型 g37
  ├ 1. A站 / gemini-3.7-flash
  ├ 2. B站 / gemini-3.7-flash
  └ 3. C站 / gemini-3.7-flash
```

客户端请求 `g37`，后台按顺序试，失败自动换下一个。**不做任何字符串相似度的自动归并** ——
各站给同一个模型起不同名字时，靠人工绑进同一个统一模型。

**两步职责**：

1. 「模型」页 = 备货：我有哪些模型（从上游勾选导入）
2. 「统一模型」页 = 上架：这些模型怎么组合、什么顺序对外提供

---

## 三、⚠️ 四个坑（都踩过，别再踩）

### 1. `auto_sync_models` 曾经默认开着

启动 45 秒后会把上游**所有**模型自动导进路由。已改为默认关，并有一次性迁移
（`db._migrate_settings` 里的 `_mig_auto_sync_off`）把老库也关掉。

### 2. 统一模型会被「自动生成」

`db.sync_groups()` 会给每个 `routes.model` 补一条 `model_groups`，
所以导入 200 个模型 = 凭空多 200 个「统一模型」。现在：

- 自动生成的标记 `auto=1`、`is_public=0`（不进 `/v1/models`）
- 「统一模型」页默认只显示 `auto=0`（手工建的），其余折叠
- **判据**：分组下只要有一个节点的 `upstream_model` 非空 → 认定是手工建的跨站统一模型

### 3. `routes` 上有 `UNIQUE(site_id, model)`

`model` 存的是**统一模型名**。所以：

- 同一个站，在同一个统一模型里**只能有一个节点**
- 想「A站的 flash 和 pro 都进同一个组」→ 现在做不到，需要改约束（见第五节）

### 4. 节点就是路由

统一模型的「节点」本质就是 `routes` 里的一行。
所以**删路由会让统一模型掉节点**，批量删除时要小心。

---

## 四、怎么在这个环境里改代码

```bash
cd /workspace/relayhub          # 已 clone 好的工作副本
/workspace/venv/bin/python -m py_compile app/*.py      # 语法检查
PY=/workspace/venv/bin/python bash tests/run_e2e.sh    # 全套端到端测试
```

`run_e2e.sh` 会自己拉起两个**假中转站**（`tests/mock_upstream.py`，
会故意返回 503 / 404 / 400 / 中途断流）+ 一个 RelayHub，跑完断言后重启一次验证持久化。

venv 里已装好 `fastapi==0.115.6` / `uvicorn==0.34.0` / `httpx==0.28.1`。

### 推送

沙箱里**没有 GitHub 凭据**，`git push` 会失败。要用带 token 的 URL：

```bash
git push "https://x-access-token:<你的token>@github.com/caojian126/relayhub.git" main
```

token 需要 `Contents: Read and write`（细粒度）或 `repo`（经典）。**不要写进仓库。**

### 工作时注意

- 输出太长会触发客户端 `stream was reset: PROTOCOL_ERROR`，**小步改、小步提交**
- 大文件别整读，用 `grep` / `sed -n 'a,bp'` 取片段
- 改完立刻 `git commit`，别攒着

---

## 五、已知限制 / 下一步可做

| # | 事项 | 说明 |
|---|---|---|
| 1 | **浏览器交互没实测过** | 沙箱没浏览器。拖拽排序、聊天测试区只做了 JS 语法解析 + `onclick` 函数存在性检查。端点行为是测过的 |
| 2 | **Zeabur 持久卷无法本地模拟** | 挂载点检测逻辑（`config.data_dir_is_mount`）本身有测试，真实 Volume 没验过 |
| 3 | 同站多模型进同一组 | 需要放开 `UNIQUE(site_id, model)` → 改成三列唯一。注意同步改 `models_sync.import_models` 和 `create_route` 里的 `ON CONFLICT` 目标 |
| 4 | 概览页每次刷新查一次 `cache.stats()` | 站点多时略慢，可合并成一次查询 |
| 5 | 语义缓存（按 embedding 相似度） | **决定不做** —— 会返回错误答案，不如把限额用足 |
| 6 | `/v1/embeddings`、`/v1/images` 转发；每 Key 速率限制 | 未做 |

---

## 六、测试覆盖（`tests/e2e.py`，17 节）

登录体检 / 站点 CRUD / 拉模型 / 统一模型与排序 / 网关 Key /
`/v1/models` 只暴露统一模型 / 普通请求故障切换 / 不可重试错误不切节点 /
模型不存在可重试 / 流式请求 / **流式已输出后断流不重试** / 导出导入 /
路由批量操作 / 自动同步默认关闭 / 自动生成分组不落外 /
批量加节点 / 挑节点只看已导入 / 改账号名后 `/me` 生效

`tests/smoke_db.py` 单独验证**老库升级**：补列、补表、数据不丢、幂等。

---

## 七、文档

| 文件 | 内容 |
|---|---|
| `README.md` | 概览、功能表、使用流程（7 步）、部署速查、测试说明 |
| `DEPLOY.md` | 完整 12 章教程：Zeabur 部署、持久卷必做步骤与验证、接入站点、统一模型、客户端配置、排错、Docker |
