# 交接文档

给接手这个仓库的下一个会话（人或 AI）看。**先读这一页，再看代码。**

当前版本 **v0.6.0**（`b0a77f4`）。上一个稳定点是 `498c3ec`（v0.5.0）。

> v0.6.0 这一轮加的东西：**「顺序」页**（站点排序 + 整站每日总次数）、
> **全局站序优先**的排序语义、**额度占位大数修复**（「剩余 1 亿」）。
> 全部已推送，工作树干净。见第二、三节。

---

## 一、这个项目是什么

把多个 AI 中转站聚合成**一个入口**：

- 客户端只配 `一个 Base URL + 一个 Key + 一个模型名`（比如 `auto`）
- 后面用哪个站、哪个真实模型，由后台决定
- 带失败自动切换、节点级冷却、每日限额、响应缓存、四种协议入口

| 项 | 值 |
|---|---|
| 仓库 | `caojian126/relayhub` |
| 技术栈 | Python 3.11 · FastAPI · httpx · SQLite(WAL) · 单文件 `index.html`（零构建） |
| 作者 | 草翦 |
| 部署 | Zeabur，**持久卷必须挂到 `/data`**，环境变量 `ADMIN_PASSWORD` / `SECRET_KEY` / `TZ` |

四个入口：`/v1/chat/completions`、`/v1/responses`、`/v1/messages`、`/v1beta/models/{m}:generateContent`。

---

## 二、v0.6.0 这一轮改了什么

从 `498c3ec` 到 `b0a77f4` 共 9 个提交，按依赖顺序：

| 提交 | 内容 |
|---|---|
| `debe740` | 调度台后端：`sites.daily_limit`（**整站**每天总次数）＋ `/admin/api/schedule{,/order,/limits}` |
| `2f8096d` | 新标签页 **「顺序」**：排站 + 每站每天总次数；删掉旧的「优先级」数字框/列 |
| `96b8c37` | **修「一亿额度」**：占位大数不当余额 ＋ 额度接口双前缀回退；新装默认调度方式改 `priority` |
| `11f1d59` | HANDOFF 更新 |
| `b8314f1` | **全局站序优先**：`model_groups.ordered`，只有手工拖过的模型才走自己的节点序 |
| `d35c279` | HANDOFF 补充「顺序」页未在真浏览器验证 |
| `ab93dc7` | 统一模型页标出「跟随全局站序 / 自己一套顺序」＋ 一键恢复跟随 |
| `f07aa74` | README / DEPLOY 同步新功能，并修两处**文档与代码不符**的旧错 |
| `b0a77f4` | 「顺序」页加 `↑↓` 按钮（手机长按拖动会被系统手势「任意门」抢走） |

### 新的核心概念：两层顺序

```
「顺序」页拖出来的全局站序   ← 默认，所有统一模型共用
        │
        └─ 在某个统一模型里手工拖过 → 该模型改听自己的（model_groups.ordered=1）
                                     列表标「自己一套顺序」，可一键恢复跟随
```

**没有 `b8314f1` 这一层，「顺序」页拖完是假动作** —— 引擎里节点优先级会压住站点优先级。

### 上一轮 v0.5.0（`ad8fb60` → `498c3ec`，11 个提交）

从 `ad8fb60` 到 `c626d05` 共 11 个提交，按依赖顺序：

| 提交 | 内容 |
|---|---|
| `a9f805f` | **统一模型**（核心）＋ 节点级轮询/冷却 ＋ 流式边界处理 ＋ 导出导入分档 |
| `5a2d48d` | 密钥默认打码 + 主动显示；新增聊天测试区（走本机网关） |
| `14b488b` | 持久化警告横幅；导出分「安全/完整」，导入分「合并/覆盖」 |
| `32293c4` | README 补统一模型用法与测试说明，修 DEPLOY.md 乱码 |
| `3099984` | DEPLOY.md 新增统一模型章节、持久卷生效验证、更新流程 |
| `c16247d` | 拉模型改为**按站点勾选导入**，默认不勾选 |
| `8754971` | **自动同步默认关闭** + 路由批量编辑 |
| `c7589b5` | 自动生成的分组不再塞满统一模型页，也不再默认暴露 |
| `db55ab2` | **批量挑选模型加节点**（不用手打上游模型名） |
| `ffc062d` | 挑节点默认只列「已导入的模型」 |
| `c626d05` | 改面板账号名后右上角立刻更新 |

### 核心概念：统一模型

```
统一模型 g37
  ├ 1. A站 / gemini-3.7-flash
  ├ 2. B站 / gemini-3.7-flash
  └ 3. C站 / gemini-3.7-flash
```

客户端请求 `g37`，后台按顺序试，失败自动换下一个。**不做任何字符串相似度的自动归并** ——
各站给同一个模型起不同名字时，靠人工绑进同一个统一模型。

**两步职责**：

1. 「模型」页 = 备货：我有哪些模型（从上游勾选导入）
2. 「统一模型」页 = 上架：这些模型怎么组合、什么顺序对外提供

---

## 三、⚠️ 六个坑（都踩过，别再踩）

### 1. `auto_sync_models` 曾经默认开着

启动 45 秒后会把上游**所有**模型自动导进路由。已改为默认关，并有一次性迁移
（`db._migrate_settings` 里的 `_mig_auto_sync_off`）把老库也关掉。

### 2. 统一模型会被「自动生成」

`db.sync_groups()` 会给每个 `routes.model` 补一条 `model_groups`，
所以导入 200 个模型 = 凭空多 200 个「统一模型」。现在：

- 自动生成的标记 `auto=1`、`is_public=0`（不进 `/v1/models`）
- 「统一模型」页默认只显示 `auto=0`（手工建的），其余折叠
- **判据**：分组下只要有一个节点的 `upstream_model` 非空 → 认定是手工建的跨站统一模型

### 3. `routes` 上有 `UNIQUE(site_id, model)`

`model` 存的是**统一模型名**。所以：

- 同一个站，在同一个统一模型里**只能有一个节点**
- 想「A站的 flash 和 pro 都进同一个组」→ 现在做不到，需要改约束（见第五节）

### 4. 节点就是路由

统一模型的「节点」本质就是 `routes` 里的一行。
所以**删路由会让统一模型掉节点**，批量删除时要小心。

### 5. 「不限额度」几乎都不是 null，而是一个大数占位

one-api / new-api 的分叉普遍把「不限」写成 `hard_limit_usd = 100000000`。
以前直接拿它算 `limit - used`，面板上就显示「剩余 1 亿」，而且所有站
看起来一样富，把 `balanced` 策略的排序也搞乱了。

现在 `quota.py` 里凡是 `>= 1e8` 或负数的上限，一律当成「不限」：
不参与计算、不喂给排序，只在 `quota_note` 里说明。

顺带两条：
- 额度接口有**两个前缀**（`/v1/dashboard/billing/*` 和 `/dashboard/billing/*`），
  老站常常只挂后者，两种都要试。
- `system_hard_limit_usd` 是「整台部署」的上限，不是账号余额，只作最后兜底。

数字对不上时，面板上「站点」页点**查额度**，弹窗里会列出取值字段 + 原始 JSON。

### 6. 顺序有两层，而 `balanced` 会让你排的顺序「看起来没生效」

两个坑叠在一起，排查时最容易绕进去：

**① 看不到效果 —— 十有八九是策略，不是没保存。**
默认调度方式是 `balanced`，它先按上游**余额**排，你排的顺序只在余额相同时
才参与比较。所以「拖完顺序没用」先看调度方式。「顺序」页会直接提示当前状态，
并给一个一键切换的链接。

> `96b8c37` 把**新装**默认改成了 `priority`，但**老库仍停在 `balanced`**。

**② 两层打架 —— `sites.priority` vs `routes.priority`。**
同名的两个东西，语义不同：

| 字段 | 是什么 | 谁写它 |
|---|---|---|
| `sites.priority` | **全局站序** | 「顺序」页（10/20/30…） |
| `routes.priority` | **某个统一模型自己的节点序** | 该模型的节点列表拖拽 |

`engine.sort_candidates` 里用 `model_groups.ordered` 决定听谁：

- `ordered=0`（默认）→ 按 `sites.priority`，即跟随全局站序
- `ordered=1` → 按 `routes.priority`，即听自己的

**只有你在「统一模型」的节点列表里手工拖过，`ordered` 才会变 1。**
想退回去，`POST /groups/{name}/order` 传 `{"follow_global": 1}`（页面上有按钮）。

⚠️ **没有 `b8314f1` 这一层，站点级排序是完全失效的** —— 节点优先级永远压住站点优先级。
如果以后有人"简化"掉 `ordered` 的逻辑，先想清楚这一点。

---

## 四、怎么在这个环境里改代码

```bash
cd /workspace/relayhub          # 已 clone 好的工作副本
/workspace/venv/bin/python -m py_compile app/*.py tests/*.py   # 语法检查
PY=/workspace/venv/bin/python bash tests/run_e2e.sh            # 全套端到端测试
```

`run_e2e.sh` 会自己拉起两个**假中转站**（`tests/mock_upstream.py`，
会故意返回 503 / 404 / 400 / 中途断流 / 占位额度）+ 一个 RelayHub，
跑完断言后重启一次验证持久化。内部还会顺带跑 `tests/js_check.py`。

venv 里已装好 `fastapi==0.115.6` / `uvicorn==0.34.0` / `httpx==0.28.1` / **`esprima`**。

单独跑前端静态检查（不需要浏览器）：

```bash
/workspace/venv/bin/python tests/js_check.py
```

### 推送

沙箱里**没有 GitHub 凭据**，`git push` 会失败。要用带 token 的 URL：

```bash
git push "https://x-access-token:<你的token>@github.com/caojian126/relayhub.git" main
```

token 需要 `Contents: Read and write`（细粒度）或 `repo`（经典）。**不要写进仓库。**

> ⚠️ **凭据不会被保存。** 内联在命令行里的 token 不会落盘，
> `~/.git-credentials` / `.netrc` / git config 里都没有（已多次全盘确认）。
> **上下文一丢就得重新找用户要** —— 别浪费时间翻磁盘，直接开口要。
> 要不到时可以让用户把提交打成 patch，自己在本地 `git am`。

### 工作时注意

- 输出太长会触发客户端 `stream was reset: PROTOCOL_ERROR`，**小步改、小步提交**
- 大文件别整读，用 `grep` / `sed -n 'a,bp'` 取片段
- 改完立刻 `git commit`，别攒着

---

## 五、已知限制 / 下一步可做

| # | 事项 | 说明 |
|---|---|---|
| 1 | **浏览器交互没实测过** | 沙箱没浏览器。前端只做到 `tests/js_check.py`（esprima 真解析 + 内联函数存在性检查），端点行为都有 e2e 覆盖。**「顺序」页的 `↑↓` 按钮和拖拽都没在真浏览器里点过。** 拖拽只让 `≡` 可拖、补了 Firefox 必需的 `dataTransfer.setData`；`↑↓` 是给手机用的（长按拖动会被系统手势「任意门」抢走） |
| 2 | **Zeabur 持久卷无法本地模拟** | 挂载点检测逻辑（`config.data_dir_is_mount`）本身有测试，真实 Volume 没验过 |
| 3 | 同站多模型进同一组 | 需要放开 `UNIQUE(site_id, model)` → 改成三列唯一。注意同步改 `models_sync.import_models` 和 `create_route` 里的 `ON CONFLICT` 目标 |
| 4 | 概览页每次刷新查一次 `cache.stats()` | 站点多时略慢，可合并成一次查询 |
| 5 | 语义缓存（按 embedding 相似度） | **决定不做** —— 会返回错误答案，不如把限额用足 |
| 6 | `/v1/embeddings`、`/v1/images` 转发；每 Key 速率限制 | 未做 |
| 7 | **空回（200 但没内容）不会被识别** | 引擎判定成功只看 HTTP 200 + 能解析 JSON（`chat.py` 的 `complete` / 流式那两处）。站点返回 200 但文字是空的会被当成**成功**：不切换节点、不冷却，客户端拿到空答案而日志里写着「成功」。<br>**用户明确说过不需要自动识别** —— 自己发现站子不出字就手动换站。真要做得先定清楚判据，注意别把「只有 `tool_calls`」或「只有 `reasoning_content`」的合法响应误杀 |
| 8 | 「顺序」页的额度不足提示 | 用户提过（把某站上移时若它今日额度已满，弹窗问「换成最近额度够的站」还是「给它加额度」），后来**主动砍掉了**，只保留了 `↑↓`。需要时可以再提 |

---

## 六、测试覆盖（`tests/e2e.py`，20 节）

登录体检 / 站点 CRUD / 拉模型 / 统一模型与排序 / 网关 Key /
`/v1/models` 只暴露统一模型 / 普通请求故障切换 / 不可重试错误不切节点 /
模型不存在可重试 / 流式请求 / **流式已输出后断流不重试** / 导出导入 /
路由批量操作 / 自动同步默认关闭 / 自动生成分组不落外 /
批量加节点 / 挑节点只看已导入 / 改账号名后 `/me` 生效 /
**调度台站点顺序与整站每日总次数** / **额度查询（路径回退、占位大数）** /
**全局站序真的决定走哪个站；统一模型可单独覆盖**

`tests/smoke_db.py` 单独验证**老库升级**：补列（含 `sites.daily_limit`、
`model_groups.ordered`、`quota_note`）、补表、数据不丢、幂等。

`tests/js_check.py` 是**不需要浏览器**的前端静态检查：用 esprima 真解析
`<script>`（报准 index.html 的行号），再核对所有内联 `on*` 里点名的函数
都有定义。`run_e2e.sh` 会调它；没装 esprima 就自动跳过（`pip install esprima`）。

---

## 七、文档

| 文件 | 内容 |
|---|---|
| `README.md` | 概览、功能表、使用流程（8 步）、调度方式与顺序、部署速查、测试说明 |
| `DEPLOY.md` | 完整 13 章教程：Zeabur 部署、持久卷必做步骤与验证、接入站点、同步模型、统一模型、**排顺序设次数**、客户端配置、缓存、运维、排错、Docker |
| `/workspace/PROGRESS.md` | **沙箱里的备忘（不在仓库内）**，断线续传用。上下文丢了先读它 |
